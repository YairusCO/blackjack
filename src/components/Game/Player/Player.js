import React from 'react'
import { Button, Grid, Typography } from '@mui/material'

const Player = ({ bank }) => {
  return <div classname="game">This is the Player page {bank}</div>
}

export default Player
